repo_type: platform
owners:
  team: architecture-platform
commands:
  check: make check
  test: make test
  lint: make lint
risk:
  external_writes: false
  secrets_required: false
  protected_paths:
    - security/**
agent_entrypoints:
  default_task: make check
