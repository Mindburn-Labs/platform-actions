name: Continuous Integration
permissions:
  contents: read

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  preflight-checks:
    uses: ./.github/workflows/agent-preflight.yml
